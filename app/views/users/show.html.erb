<h1>Welcome To Your Brew Bank <%= @user.username %>!</h1>

  <% @user.reviews.each do |r| %>
  <p><%= r.beer.name %></p>
  <p><%= link_to r.title, review_path(r.id) %></p>
  <p>Rating: <%= r.rating %></p>
  <p><%= r.content %></p><br>
  <% end %>

