<%= render partial: 'layouts/errors', locals: {object: @review}  %>

<%= render partial: 'form'%><br>

<%= button_to 'Delete Review', review_path(@review), method: :delete, class: "btn btn-primary" %>